New features:
- the iqt module works now also on Windows (thanks to an auxiliary module).
- one of the QwtPlotPicker::TrackerText() C++ member functions has been renamed
  to QwtPlotPicker.TrackerTextF() in Python for disambiguation.
- works with Qt-4.3.0 in combination with recent SIP and PyQt snapshots.

Bug fixes:
- builts against an system Qwt library without the QwtSVGItem CONFIG option.
- includes revision 144 of the Qwt subversion bugfix branch.